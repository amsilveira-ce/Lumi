# Google Cloud / Vertex AI Configuration
# Replace these with your actual Google Cloud project details
GOOGLE_CLOUD_PROJECT=your-project-id-here
GOOGLE_CLOUD_REGION=us-central1
GOOGLE_APPLICATION_CREDENTIALS=/path/to/your/service-account-key.json

# Vertex AI Model Configuration
# Available models: gemini-1.5-pro, gemini-1.5-flash, gemini-1.0-pro
VERTEX_AI_MODEL=gemini-1.5-pro
VERTEX_AI_LOCATION=us-central1


# MCP Server Configuration
MCP_SERVER_HOST=localhost
MCP_SERVER_PORT=8080

# Memory Configuration
# Options: local, cloud (future support)
MEMORY_BACKEND=local
MEMORY_PATH=./memory/conversations

# Optional: API Keys for additional services
# Uncomment and add keys if you want to use these services
# OPENAI_API_KEY=sk-...
# ANTHROPIC_API_KEY=sk-ant-...

# Logging Configuration
LOG_LEVEL=INFO
LOG_FILE=./logs/elder-companion.log

# ============================================
# SETUP INSTRUCTIONS:
# ============================================
# 1. Replace 'your-project-id-here' with your actual Google Cloud project ID
# 2. Update the path to your service account JSON key file
# 3. Ensure you have enabled the following APIs in Google Cloud:
#    - Vertex AI API (aiplatform.googleapis.com)
#    - Generative Language API (generativelanguage.googleapis.com)
# 4. Save this file as '.env' (remove .example extension if needed)
# ============================================